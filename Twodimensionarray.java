package Arrays;

public class Twodimensionarray {
    public static void main(String args[]){
        int[][] a = {{1,2,4},
                    {5,6,7,8},
                     {9,10}
                    };
    System.out.println("1st array's length in multidimen array is " + a[0].length);
    System.out.println("1st array's length in multidimen array is " + a[1].length);
    System.out.println("1st array's length in multidimen array is " + a[2].length);
    // the length attribute used to calculate the length of each row.
    }
    
    
}
